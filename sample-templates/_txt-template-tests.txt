
1)  simple tags
2)  bracket escaping
3)  complex bind expressions
4)  bind object support (dynamic, simple poco, dictionary)
5)  throw on no item exists (bind error)
6)  truthy/falsey
7) simple conditional blocks
8) negated conditonals
x) simple iteration blocks
x) null/empty IEnumerable targets
x) $ scope
x) walking the scope chain ..\..\
x) simple partial blocks
x) simple template comments
x) multi-line template comments (todo: test comments containing brackets)
x) simple whitespace control ({#if}{/if}, {#each}{/each}, {!comment}, {>partial})
x) global whitespace control ({#if}{/if}, {#each}{/each}, {!comment}, {>partial})
x) simple lambda expressions
x) complex lambda expressions (multi-param, string literals, numeric literals)a
x) lamba expression driven block tags ({#if}, {#each}, {>partial})
